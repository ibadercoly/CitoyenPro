/**
 * Couche d'accès réseau : interfaces Retrofit (`*Api`) décrivant les endpoints,
 * et DTO (`*Dto`) représentant le format JSON échangé avec le backend.
 * Les DTO sont mappés vers des modèles métier ([com.ibader.citoyenpro.domain.model])
 * avant de remonter au repository.
 */
package com.ibader.citoyenpro.data.remote
